{
    "scripts": {
        "build": "honkit build && node scripts/fix-viewport.mjs",
        "serve": "honkit serve"
    },
    "devDependencies": {
        "honkit": "^6.1.7",
        "honkit-plugin-tbfed-pagefooter": "^0.0.8"
    },
    "dependencies": {
        "honkit-plugin-katex": "^1.1.7"
    }
}
